[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# 静态导出站点的兜底：所有路径回退到首页（仅当路由无独立 index.html 时生效）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
